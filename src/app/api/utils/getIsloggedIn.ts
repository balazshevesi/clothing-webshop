import { cookies } from "next/headers";
import { NextResponse, NextRequest } from "next/server";

import jwt from "jsonwebtoken";

export function getIsloggedIn(authorizationToken) {

  return userId;
}
